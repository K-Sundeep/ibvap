# IBVAP environment variables (copy to .env and fill in per-service as needed)

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
DB_PATH=./db/ibvap.db

# Ingest
INGEST_HOST=0.0.0.0
INGEST_PORT=8001

# Inference
INFERENCE_HOST=0.0.0.0
INFERENCE_PORT=8002
GPU_DEVICE=0
BACKEND_URL=http://localhost:8000

# Frontend
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Alerting (Phase 2+, fill in when wired up)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
