// TODO Phase 2 (THE FLOOR): live alert stream via WebSocket, shows explainable
// alert text (e.g. "track crossed virtual line", "plate matched blacklist").
export default function AlertsFeed() {
  return null;
}
